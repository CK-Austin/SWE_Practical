

#include <iostream>
#include "Answers.h"
#include "Questions.h"

int main()
{
	srand(time(NULL));
	//std::cout << "Hello user, let's play a game of random trivia";
	Questions q;
	q.DisplayQuestion();
	
}







//Create a C++ project that displays the user a random multiple choice question.There should be at least 4 possible questions.
//Prompt the user for the answerand tell them if they are correct or not.
